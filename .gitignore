# editors
.vscode
*.swp
*~

# python
.venv

# build
/build*
/twister-out*

# native_sim flash-simulator backing files. Every binary defaults to
# ./flash.bin; the docs ask for a per-app name because a store written in
# one app's flash geometry is unreadable by another.
*.bin

__pycache__/

# docs
/doc/_build*
