# Copyright (c) 2026
# SPDX-License-Identifier: Apache-2.0

mainmenu "blob_db ztest"

menu "Zephyr"
source "Kconfig.zephyr"
endmenu

# The blob_db library's Kconfig (CONFIG_BLOB_DB and friends) is contributed by
# the repository's module Kconfig entry point (zephyr/module.yml -> Kconfig ->
# rsource lib/Kconfig), so it must not be sourced again here.
