# Copyright (c) 2021 Nordic Semiconductor ASA
# SPDX-License-Identifier: Apache-2.0

manifest:
  self:
    west-commands: scripts/west-commands.yml

  remotes:
    - name: zephyrproject-rtos
      url-base: https://github.com/zephyrproject-rtos
    - name: kamil-kielbasa
      url-base: https://github.com/kamil-kielbasa

  projects:
    - name: zephyr
      remote: zephyrproject-rtos
      revision: main
      import:
        # By using name-allowlist we can clone only the modules that are
        # strictly needed by the application.
        name-allowlist:
          - cmsis_6    # required by the ARM port for Cortex-M
          - hal_nordic # required by the custom_plank board (Nordic based)
          - hal_stm32  # required by the nucleo_f302r8 board (STM32 based)
    # UBI flash-virtualization layer — provides the wear-leveled volume
    # substrate that the key-value store builds on. Pinned to the stable
    # v1.0.0 release (public API + on-flash format frozen).
    - name: ubi
      remote: kamil-kielbasa
      revision: v1.0.0
      path: ubi
