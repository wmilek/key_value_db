# Nothing to configure beyond the defaults: this app links no storage stack,
# so there is no sector buffer to size and no UBI pool to match to the part.
# The board overlay moves storage_partition to the on-board MX25R64.
