/*
 * Same 16 MB sim-flash / 8 MB storage_partition layout as the main app,
 * so the benchmark exercises multi-bucket geometry on native_sim too.
 */

&flashcontroller0 {
	reg = <0x00000000 DT_SIZE_M(16)>;
};

&flash0 {
	reg = <0x00000000 DT_SIZE_M(16)>;
};

&storage_partition {
	reg = <0x00800000 DT_SIZE_M(8)>;
};
